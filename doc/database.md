# Setting up the database
The server uses a MySQL database which needs to have been populated with the data from sql files that L2J distributes.

The configuration that the server reads to connect to it is located at ```Server.properties```.
